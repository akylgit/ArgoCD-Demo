apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 5  # Number of pods to deploy
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: akyldocker25/apache-ne:latest
        ports:
        - containerPort: 80  # Change container port to 80
